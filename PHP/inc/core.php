<?php

namespace Movies;

require_once ('global.php');
class Core extends Users
{

    function load($url) {

        if(!isset($_SESSION ['id'])) {
            switch ($url) {
                case 'register':
            }
        }
    }
}